/*
@author: Christian Davis
@date: 03/02/2025
@purpose: The ArrayOperations.java program for CSC_151
*/

package labs.example.arrays;

public class ArrayOperations
{
    public static int void createNewArray(int array_length)
    {
        int[]first_array = new int [array_length];
        for (int x = 0; x < first_array; x++)
        {
            hours[x] = x;
        }
        private static int void displayArray(int[]first_array)
        {
            System.out.println("I create a new array and it now has " + array_length + " items in it.\n");
            System.out.println("The array items and their values are listed below: ")
            for (int index = 0; index < array_length; index++)
            {
                System.out.println(first_array[index]);
            }
        }
    }
}